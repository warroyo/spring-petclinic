apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ template "petclinic.fullname" . }}
  labels:
    app: {{ template "petclinic.name" . }}
    version: {{ .Values.image.buildversion }}
    chart: {{ template "petclinic.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ template "petclinic.name" . }}
      release: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app: {{ template "petclinic.name" . }}
        version: {{ .Values.image.buildversion }}
        release: {{ .Release.Name }}
    spec:
      imagePullSecrets:
      - name: {{ .Values.secret}}
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.buildversion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          env: {{ .Values.environment }}
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /
              port: http
          readinessProbe:
            httpGet:
              path: /
              port: http
